/*Realizar un script 04-usuario.html que solicite una dirección de correo electrónico y 
  la trocee para quedarse solo con el nombre de usuario (lo que va antes de la @) para 
  mostrarlo al usuario. Elimina los posibles espacios que pueda tener con trim.*/


/*
const email=prompt("Correo electronico: ");

if (isNaN(email)&&(email.indexOf("@")!==-1)){
    const usuario = email.slice(0, email.indexOf("@"));
    console.log(usuario.trim());
} else {
    console.log("El valor introducido no es valido.");
};
*/


